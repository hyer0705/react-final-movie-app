import { LoaderWrapper, LoadingSpinner } from "../common/common-components";

function Loader() {
  return (
    <LoaderWrapper>
      <LoadingSpinner />
    </LoaderWrapper>
  );
}

export default Loader;
